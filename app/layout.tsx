import { ReactNode } from "react";

export const metadata = {
  title: "Keihan Jafari - Frontend Developer",
  description:
    "Professional CV of Keihan Jafari, Frontend Developer specializing in React, Next.js, and TypeScript",
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return children;
}
